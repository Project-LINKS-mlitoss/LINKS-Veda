export const DEFAULT_MARGINS = {
	top: 20,
	right: 20,
	bottom: 20,
	left: 20,
};

export const DEFAULT_SCATTER_CONFIG = {
	fill: "#8884d8",
	shape: "circle" as string, // Recharts can accept built-in shapes or custom
};
